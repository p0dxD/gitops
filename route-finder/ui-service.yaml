apiVersion: v1
kind: Service
metadata:
  name: route-finder-ui
  namespace: route-finder
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.50.85
  selector:
    app: route-finder-ui
  ports:
    - port: 80
      targetPort: 8080
