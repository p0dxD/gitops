apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: umami-postgres-pvc
  namespace: umami
  labels:
    recurring-job.longhorn.io/source: enabled
    recurring-job-group.longhorn.io/backup-postgres: enabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
